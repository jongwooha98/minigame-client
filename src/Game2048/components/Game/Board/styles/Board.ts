/**
 * The number of tiles per row and column.
 * IMPORTANT! Check styles before changing this value!
 *
 * @constant {number}
 */
export const tileCountInRow = 4;

/**
 * The board margin (left, right, top, bottom).
 */
export const boardMargin = `1.25rem`;
